<!--
  OAuth + Rate Limiting + Content Safety policy
  Full protection stack for MCP servers
-->
<policies>
  <inbound>
    <base />
    <!-- Validate OAuth token -->
    <validate-azure-ad-token tenant-id="{{tenant-id}}" 
                              failed-validation-httpcode="401" 
                              failed-validation-error-message="Unauthorized">
      <audiences>
        <!-- Use appId directly when identifierUris is empty -->
        <audience>{{mcp-app-client-id}}</audience>
      </audiences>
    </validate-azure-ad-token>
    <!-- Extract MCP session ID for rate limiting -->
    <set-variable name="mcp-session-id" value="@(context.Request.Headers.GetValueOrDefault("Mcp-Session-Id", context.Request.Headers.GetValueOrDefault("X-Request-Id", Guid.NewGuid().ToString())))" />
    <!-- Rate limit: 10 requests per minute per session -->
    <rate-limit-by-key calls="10" renewal-period="60" counter-key="@((string)context.Variables["mcp-session-id"])" />
    <!-- Content Safety check using llm-content-safety policy -->
    <llm-content-safety backend-id="content-safety-backend" shield-prompt="true">
      <categories output-type="EightSeverityLevels">
        <category name="Hate" threshold="4" />
        <category name="Violence" threshold="4" />
        <category name="Sexual" threshold="4" />
        <category name="SelfHarm" threshold="4" />
      </categories>
    </llm-content-safety>
  </inbound>
  <backend>
    <forward-request />
  </backend>
  <outbound>
    <base />
  </outbound>
  <on-error>
    <base />
    <choose>
      <when condition="@(context.Response.StatusCode == 401)">
        <return-response>
          <set-status code="401" reason="Unauthorized" />
          <set-header name="WWW-Authenticate" exists-action="override">
            <!-- Dynamic: uses context.Api.Path for any MCP API (trim leading slash) -->
            <value>@("Bearer error=\"invalid_token\", resource_metadata=\"" + "{{apim-gateway-url}}/" + context.Api.Path.TrimStart('/') + "/.well-known/oauth-protected-resource\"")</value>
          </set-header>
          <set-body>@{
            var gatewayUrl = "{{apim-gateway-url}}";
            var apiPath = context.Api.Path.TrimStart('/');
            return JsonConvert.SerializeObject(new {
              error = "unauthorized",
              message = "Valid OAuth token required",
              resource_metadata = gatewayUrl + "/" + apiPath + "/.well-known/oauth-protected-resource"
            });
          }</set-body>
        </return-response>
      </when>
      <when condition="@(context.Response.StatusCode == 429)">
        <return-response>
          <set-status code="429" reason="Too Many Requests" />
          <set-header name="Retry-After" exists-action="override">
            <value>60</value>
          </set-header>
        </return-response>
      </when>
      <when condition="@(context.LastError.Source == "llm-content-safety")">
        <return-response>
          <set-status code="400" reason="Bad Request" />
          <set-body>@{
            return JsonConvert.SerializeObject(new {
              error = "content_blocked",
              message = "Request blocked by content safety policy"
            });
          }</set-body>
        </return-response>
      </when>
    </choose>
  </on-error>
</policies>
