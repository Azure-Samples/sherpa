<!--
  OAuth + Rate Limiting policy
  Validates tokens and applies rate limiting by subscription key
-->
<policies>
  <inbound>
    <base />
    <!-- Rate limit FIRST: 10 requests per minute per subscription key -->
    <rate-limit-by-key calls="10" renewal-period="60" counter-key="@(context.Subscription.Id)" />
    <!-- Then validate OAuth token -->
    <validate-azure-ad-token tenant-id="{{tenant-id}}" 
                              failed-validation-httpcode="401" 
                              failed-validation-error-message="Unauthorized">
      <audiences>
        <audience>api://{{mcp-app-client-id}}</audience>
      </audiences>
    </validate-azure-ad-token>
  </inbound>
  <backend>
    <forward-request />
  </backend>
  <outbound>
    <base />
  </outbound>
  <on-error>
    <base />
    <choose>
      <when condition="@(context.Response.StatusCode == 401)">
        <return-response>
          <set-status code="401" reason="Unauthorized" />
          <set-header name="WWW-Authenticate" exists-action="override">
            <value>Bearer error="invalid_token", resource_metadata="{{apim-gateway-url}}/.well-known/oauth-protected-resource"</value>
          </set-header>
        </return-response>
      </when>
      <when condition="@(context.Response.StatusCode == 429)">
        <return-response>
          <set-status code="429" reason="Too Many Requests" />
          <set-header name="Retry-After" exists-action="override">
            <value>60</value>
          </set-header>
          <set-body>@{
            return JsonConvert.SerializeObject(new {
              error = "rate_limit_exceeded",
              message = "Too many requests. Please wait before retrying.",
              retry_after = 60
            });
          }</set-body>
        </return-response>
      </when>
    </choose>
  </on-error>
</policies>
